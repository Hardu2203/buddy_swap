import 'package:flutter/material.dart';

class BuyDetailsScreen extends StatelessWidget {
  const BuyDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Buy Details Screen"),),
    );
  }
}
